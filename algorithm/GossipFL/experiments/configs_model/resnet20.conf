lr="${lr:-0.1}"
batch_size="${batch_size:-32}" # 32 128 256 after testing , 32 is the best bs
wd="${wd:-0.0001}"
momentum="${momentum:-0.9}"
model="${model:-resnet20}"
# epochs="${epochs:-300}"
# comm_round="${comm_round:-300}"
epochs="${epochs:-200}"
comm_round="${comm_round:-200}"
